# Fairway One Cloud

Fairway One uses its dedicated Supabase project for accounts, courses, events, rounds, teams, scoring and custom round segments.

Database changes are applied as versioned migrations. Row Level Security protects user and event data, while realtime scoring is enabled for active round data.
